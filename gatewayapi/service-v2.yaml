apiVersion: v1
kind: Service
metadata:
  name: svc-v2
  namespace: default
  labels:
    app: avi-hello
    version: v2
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: avi-hello
    version: v2